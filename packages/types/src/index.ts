// Shared types and Zod schemas that cross the network between apps/web and apps/api.
// Real schemas are added per-slug as routes are built.

export {} // placeholder until the first shared type lands
